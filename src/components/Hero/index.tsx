import { StyledHero, HeroTitle } from './StyledHero'; 

const Hero = () => (
  <StyledHero>
    <div className="hero-content">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </StyledHero>
);

export default Hero;
